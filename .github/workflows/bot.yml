name: 🤖 Mastodon Simple Bot

on:
  schedule:
    # Uruchamiaj co 30 minut, 24/7
    - cron: '*/30 * * * *'
  
  workflow_dispatch:

concurrency:
  group: mastodon-simple-${{ github.ref }}
  cancel-in-progress: true

jobs:
  run-bot:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    
    steps:
    - name: 📥 Pobierz kod
      uses: actions/checkout@v3
    
    - name: 🐍 Ustaw Pythona
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: 📦 Zainstaluj zależności
      run: pip install Mastodon.py==1.8.1
    
    - name: 🤖 Uruchom bota
      env:
        MASTODON_ACCESS_TOKEN: ${{ secrets.MASTODON_ACCESS_TOKEN }}
        MASTODON_BASE_URL: ${{ secrets.MASTODON_BASE_URL }}
      run: python bot.py
    
    - name: 💾 Zapisz zmiany
      if: always()
      run: |
        git config --global user.email "bot@github.com"
        git config --global user.name "GitHub Bot"
        
        git add used_sentences.json counter.txt posted_toots.json 2>/dev/null || true
        
        if git diff --staged --quiet; then
          echo "📭 Brak zmian"
        else
          git commit -m "🤖 Update $(date -u +'%H:%M')"
          git push
          echo "✅ Zmiany zapisane"
        fi
